#include<bits/stdc++.h>

using namespace std;

int main(){

    set< pair<int, int> > s; //set of pairs

    s.insert({2, 3});
    s.insert({10, 20});
    s.insert({30, 400});
    s.insert({401, 500});

    // pair {a,b} is smaller than {c,d} iff (a < b) or (a == b and c < d)

    

    return 0;
}